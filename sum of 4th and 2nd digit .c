#include<stdio.h>
int main()
{
	int a,b,c,d;
    //so the number is abcd.
    printf("Give the four digit number : ",a,b,c,d);
    scanf("%d%d%d%d",&a&b&c&d);

    int sum;
    sum=b+d;
    //sum=4th digit+2nd digit
    ptintf("The sum of 4th and 2nd digit is :%d",sum);
    
    return 0;
	
}
